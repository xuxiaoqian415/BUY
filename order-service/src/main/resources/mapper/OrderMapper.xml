<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zust.buy.order.mapper.OrderMapper">
    
    <resultMap id="orderResult" type="com.zust.buy.common.entity.Order">
        <association property="wxUserInfo" column="user_id"
                     select="com.zust.buy.wxpreorder.mapper.WxUserInfoMapper.getByOpenId"/>
    </resultMap>

    <select id="getOrderList" parameterType="java.util.Map" resultMap="orderResult">
        select *
        from t_order
        <where>
            <if test="orderNo != null and orderNo != ''">
                and order_no like concat('%', #{orderNo}, '%')
            </if>
        </where>
        order by create_time desc
        <if test="start != null and pageSize != null">
            limit #{start}, #{pageSize}
        </if>
    </select>

</mapper>
