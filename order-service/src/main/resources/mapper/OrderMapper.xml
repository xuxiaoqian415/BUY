<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zust.buy.order.mapper.OrderMapper">

    <select id="getOrderList" parameterType="java.util.Map" resultType="com.zust.buy.common.entity.Order">
        select o.*, wui.nick_name
        from t_order o
            left join t_wx_user_info wui on o.user_id = wui.open_id
        <where>
            o.deleted = 0
            <if test="orderNo != null and orderNo != ''">
                and order_no like concat('%', #{orderNo}, '%')
            </if>
        </where>
        order by o.create_time desc
        <if test="start != null and pageSize != null">
            limit #{start}, #{pageSize}
        </if>
    </select>

    <select id="getTotal" parameterType="java.util.Map" resultType="int">
        select count(*)
        from t_order o
        <where>
            deleted = 0
            <if test="orderNo != null and orderNo != ''">
                and order_no like concat('%', #{orderNo}, '%')
            </if>
        </where>
    </select>
</mapper>
